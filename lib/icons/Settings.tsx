import { Settings as LucideSettings } from 'lucide-react-native';
import { iconWithClassName } from './iconWithClassName';
const Settings = iconWithClassName(LucideSettings);
export default Settings;
